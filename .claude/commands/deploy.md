---
description: Запустить один этап DEPLOY — DevOps готовит и выполняет развёртывание.
argument-hint: <slug фичи>
---

Вызови субагента `devops` через Agent tool для фичи `$ARGUMENTS` (папка `docs/features/$ARGUMENTS/`). Он подготовит артефакты деплоя и `05-deploy.md`, обновит `pipeline.md`. Боевой деплой в прод (живые пользователи на ourdiary.love), изменения прод-данных DynamoDB и платную/необратимую инфру оставь за подтверждением PO (`[NEEDS-CEO-APPROVAL]`). После — верни резюме: что готово, нужные секреты/SSM, что требует подтверждения.
