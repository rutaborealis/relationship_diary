---
description: Дашборд — статус всех фич в работе и что ждёт решения.
---

Ты — оркестратор команды relationship_diary. Покажи статус по всем фичам.

1. Найди все `docs/features/*/pipeline.md` (кроме `_template`).
2. Для каждой фичи определи: slug, текущий этап и статусы (analyze/design/build/qa/deploy), общий прогресс.
3. Собери всё, что требует внимания PO: пункты `[BLOCKER]`, `[NEEDS-CEO-APPROVAL]`, фичи в `blocked`, фичи где QA = FAIL.
4. **Пересобери доску `docs/BACKLOG.md`** из этих данных: актуализируй секции «🚧 В работе», «⛔ Заблокировано», «✅ Готово» (источник истины — `pipeline.md`, идеи в «💡 Идеи» не трогай).
5. Выведи краткую сводку в чат и предложи следующий шаг по каждой активной фиче (`/ship <slug>`, `/analyze <slug>` и т.п.).
