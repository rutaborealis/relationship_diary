# Pipeline

- **feature:** password-reset
- **created:** 2026-06-30

## Этапы
Статусы: `pending` → `ready` → `in_progress` → `done` · `blocked` · `failed`

| Этап | Роль | Статус | Артефакт |
|---|---|---|---|
| analyze | business-analyst | done    | 01-requirements.md |
| design  | architect        | done    | 02-spec.md |
| build   | dev-lead         | done    | 03-build-notes.md + frontend/ backend/ |
| qa      | qa-engineer      | done    | 04-test-report.md (PASS — 14/14 AC; прогон 2 после фикса) |
| security| security-engineer| done    | 04b-security-review.md (PASS-WITH-NOTES — SEC-01 закрыт; 6 low/info) |
| deploy  | devops           | done    | 05-deploy.md (боевой выкат выполнен 2026-07-01 после одобрения PO) |

## Журнал
- 2026-06-30 intake: фича заведена. Self-service сброс пароля по email (Resend), как у верификации почты. Ключевые требования безопасности: не раскрывать существование email, одноразовый код/токен с TTL, защита от перебора, инвалидация сессий после смены пароля.
- 2026-06-30 analyze (business-analyst): написан 01-requirements.md — 20 FR, 14 acceptance criteria (Given/When/Then), NFR с акцентом на безопасность (anti-enumeration, криптостойкий одноразовый код+TTL, лимит попыток+rate limiting, инвалидация сессий, аудит без секретов). 8 допущений. PO-блокеров нет. Открытые вопросы инженерные (к архитектору/безопаснику): механизм отзыва stateless-JWT после смены пароля (FR-14), нужна ли капча сверх rate limiting, информ-письмо «пароль изменён». analyze→done, design→ready.
- 2026-06-30 build (dev-lead): реализованы T-1..T-12 в ветке `feat/password-reset`. Backend: `lib/secure-code.ts` (crypto.randomInt, sha256, timing-safe), `tv`/tokenVersion в jwt+requireAuth+login+verify-email (ADR-0002), эндпоинты `request-reset` (anti-enumeration, RESPONSE_FLOOR_MS, rate-limit RESET#<email>/RL 5/час+1/60с, SHA-256 код TTL 15м) и `confirm-reset` (timing-safe, atomic capped attempts, atomic passwordHash+tokenVersion, code consume, best-effort письмо), пороги в config. Frontend: ForgotPassword/ResetPassword страницы, ссылка с LoginPage, маршруты, api.requestReset/confirmReset. Infra: 2 Lambda + POST-маршруты. 4 атомарных коммита. Проверки зелёные: tsc backend, frontend build, sam validate --lint, unit secure-code. 2 минорных [SPEC-DEVIATION] (tokenVersion via if_not_exists+1 вместо ADD для корректной инвалидации неявной v1; доп. поле lastAt в RL). Полный локальный e2e оставлен QA (DynamoDB Local не поднят, :3000 занят). build→done, qa→ready.
- 2026-06-30 security (security-engineer): написан 04b-security-review.md. Вердикт **FAIL**: 0 critical / 1 high / 0 medium / 6 low-info. [SEC-01] HIGH — лимит «5 попыток на код» (FR-9) обходится конкурентными запросами: в `confirm-reset` сравнение кода не зашторено счётчиком попыток (timing-safe match выполняется на каждом запросе до атомарного `ADD attempts`), а собственного rate-limit у `confirm-reset` нет → N параллельных догадок на код (до конкурентности Lambda, не 5); при коде 10^6 это реальный путь подбора/захвата аккаунта. Плюс off-by-one (фактически 6 попыток) и success-path игнорирует счётчик. Фикс: гейтить сравнение успешным условным инкрементом + RL на confirm-reset. Прочее PASS: anti-enumeration (ответ+floor), crypto.randomInt, SHA-256 хэш+TTL+timing-safe, инвалидация JWT через tokenVersion (ADR-0002, if_not_exists+1 корректен, регрессий нет), bcrypt+серверная политика пароля, нет инъекций/IDOR, секреты не текут. Low: остаточный timing-leak при медленном Resend [SEC-03], `verifyToken` без `algorithms` пиннинга [SEC-04], шире-чем-нужно IAM SSM diary/* [SEC-05], тайминг наличия активного кода [SEC-06], долг Math.random/плейн-код в register/verify-email [SEC-07], неатомарный RL request-reset [SEC-08]. Динамический PoC не гонялся (нет локального стека). security→done; деплой блокирован до устранения [SEC-01].
- 2026-06-30 security (security-engineer, прогон 2 — повторное ревью после фикса): вердикт **PASS-WITH-NOTES**. **[SEC-01] HIGH закрыт.** Перепроверено по коду `confirm-reset.ts`: сравнение кода теперь зашторено атомарным условным инкрементом — `UpdateItem ADD attempts :one ConditionExpression attempts < :max` (`:77-83`) выполняется ПЕРЕД timing-safe сравнением (`:96`), и сравнение структурно недостижимо без успешного инкремента (любой путь catch → throw). DynamoDB сериализует условные апдейты на одном item → при любой конкуренции ≤5 сравнений на код. `attempts` инициализируется 0 (`request-reset.ts:70`), условие `attempts<5` пропускает {0,1,2,3,4} = ровно 5 (off-by-one устранён); 6-й запрос → `ConditionalCheckFailedException` (`:87`) → код удалён. Success-path проходит тот же гейт (верная догадка после лимита отвергается). Прочие ошибки `throw err` (`:91`) — не маскируются под лимит. Добавлен per-email confirm RL `RESET#<email>/RLC` 20/ч (`config.auth.resetConfirmRateLimitPerHour`), нейтральный `INVALID_CODE` до lookup → без enumeration. Регрессий по ранее-PASS пунктам нет: anti-enumeration (request-reset неизменён), crypto.randomInt+SHA-256+timing-safe, TTL/одноразовость, инвалидация JWT через tokenVersion (if_not_exists+1), bcrypt rounds=12, нет инъекций/IDOR, секреты не текут. Остаток: 0 critical/high/medium, 6 low/info (SEC-03..08, все accepted/вне-scope, не блокеры); confirm RL неатомарен (расширение SEC-08, low — defence-in-depth поверх жёсткого per-code cap, SEC-01 не переоткрывает). Динамический PoC брутфорса не гонялся (нет локального стека); dev-lead сообщает о локальной верификации 15/15 PASS. security→done. Деплой по линии безопасности разблокирован; финальная готовность к deploy зависит также от параллельной QA-перепроверки (строку qa не трогал).
- 2026-06-30 design (architect): написан 02-spec.md. Решение — 6-значный криптостойкий код (crypto.randomInt, не Math.random), сущность RESET#<email>/CODE (SHA-256 хэш кода, TTL 15 мин, attempts) + RESET#<email>/RL (rate limit 5/час), без нового GSI/таблицы. Эндпоинты: POST /api/auth/request-reset (anti-enumeration + постоянное время RESPONSE_FLOOR_MS) и POST /api/auth/confirm-reset (timing-safe, лимит 5 попыток, atomic update passwordHash+tokenVersion). FR-14 — отзыв JWT через tokenVersion в профиле, проверяется в requireAuth → ADR-0002. Фронт: ForgotPasswordPage + ResetPasswordPage + ссылка с LoginPage, после сброса редирект на /login. Инфра: 2 новых Lambda (Crud+SSM). Капча отклонена для v1, FR-17 письмо включено. 12 задач T-1..T-12. [NEEDS-BA] нет. design→done, build→ready.
- 2026-06-30 qa (qa-engineer): прогнан e2e против AC-1..AC-14 на локальном стеке (DynamoDB Local + Express-обёртка на :3001, Resend замокан в консоль; статика tsc backend + frontend build зелёные). Закрыто 13/14 AC: anti-enumeration (одинаковый body+тайминг 0.60–0.62с для verified/несуществующего/не-verified, письмо только verified), одноразовость+TTL, resend-инвалидация, rate limiting (60с-интервал + 5/час, нейтральный ответ), смена пароля (старый пароль→401, новый→ок), инвалидация JWT (старый токен→401, tokenVersion 1→2), серверная политика пароля, отсутствие секретов в логах, ссылка/маршруты на фронте. ВЕРДИКТ FAIL: блокирует `[BUG-1]` (medium, security) — лимит попыток off-by-one: после 5 неверных попыток код НЕ инвалидируется и верный код всё ещё принимается (удаление лишь на 6-й неверной); success-ветка `confirm-reset` не проверяет `attempts`. AC-8 не закрыт. qa→done; дефект возвращается лиду на фикс. (security-строку не трогал — параллельный агент.)
- 2026-06-30 deploy (devops): написан 05-deploy.md. Все readiness-проверки зелёные: `npx tsc -p backend/tsconfig.json --noEmit` OK, `frontend npm run build` OK (JS 285КБ), `sam validate --lint` valid, `sam build` Build Succeeded (обе новые функции AuthRequestResetFunction/AuthConfirmResetFunction собраны). Объём — **backend + frontend (оба)**: backend несёт новые Lambda+маршруты (request/confirm-reset) и изменения общих lib (auth-middleware tv-gate, jwt tv, login/verify-email, secure-code, config), фронт — страницы ForgotPassword/ResetPassword + ссылка/маршруты. **Новых SSM/секретов нет** — reset использует существующие `/diary/resend-api-key` (через ses.ts) и `/diary/jwt-secret`. **Совместимость сессий при tokenVersion подтверждена по коду:** старые JWT без `tv` и профили без `tokenVersion` оба трактуются как 1 (`auth-middleware.ts:18` `?? 1`), 1===1 → действующие сессии НЕ разлогиниваются; инкремент v1→v2 (`if_not_exists+1`) происходит только при реальном сбросе → миграция не нужна. Ранбук: порядок backend→frontend (эндпоинты до выката UI; backend обратносовместим со старым фронтом), CloudFront-инвалидация `/*` встроена в deploy-frontend.sh, smoke (ссылка→письмо/код в проде→сброс→старый токен 401, второй юзер не разлогинен). Откат: пред-релизный sam deploy / пере-выклад фронта; tokenVersion обратно совместим (необратимых изменений данных нет). Наблюдаемость: Errors request/confirm-reset, всплески 400/INVALID_CODE, доставка Resend, 401 от tokenVersion. **Боевой деплой НЕ выполнялся** (живые пользователи-пара). deploy→**done** (подготовка завершена, всё зелёное); боевой выкат ждёт `[NEEDS-CEO-APPROVAL]` на (1) merge `feat/password-reset`→main — лессон проекта: деплой собирает из рабочего дерева, поэтому только из влитого/чистого состояния; (2) `bash scripts/deploy-backend.sh`; (3) `bash scripts/deploy-frontend.sh`. 6 low/info находок безопасности — accepted тех-долг, не блокеры.
- 2026-07-01 deploy (боевой выкат, одобрен PO): мерж `feat/password-reset`→`main` (`--no-ff`, запушен), `deploy-backend.sh` (SAM `relationship-diary`, eu-central-1, стек UPDATE_COMPLETE; новые Lambda `diary-auth-request-reset-prod` + `diary-auth-confirm-reset-prod` подтверждены), `deploy-frontend.sh` (S3 + CloudFront-инвалидация, https://ourdiary.love). AWS-аккаунт `049710942442` (user `aws_learner_root`). Smoke: `request-reset` с несуществующим email → `200` нейтральный текст (anti-enumeration); `confirm-reset` с неполным телом → `400` graceful (не 500). Доска → «✅ Готово». deploy→done.
- 2026-06-30 qa (qa-engineer, прогон 2 — перепроверка после фикса BUG-1/SEC-01): вердикт **PASS**, 14/14 AC. Статика зелёная (tsc backend, frontend build). Прицельная перепроверка AC-8 + регрессия — прямым вызовом хендлеров против DynamoDB Local (детерминированный контроль `attempts`/RLC/TTL; :3000 занят, Express-обёртка резерв на :3001), харнесс 32/32 PASS. **AC-8 FAIL→PASS:** атомарный `ADD attempts ConditionExpression attempts<5` стал гейтом ДО timing-safe сравнения; TEST A — 5 неверных → `400`×5, `attempts:5`, 6-я с ВЕРНЫМ кодом → `400` + код удалён, пароль не изменён (`tv:1`); TEST B — 4 неверных + ВЕРНЫЙ на 5-й → `200`, `tv:2` (ровно 5 попыток, не 6); success-path проходит тот же гейт. Новый confirm RL `RESET#<email>/RLC` 20/ч подтверждён (TEST C: #20 разрешён → `200`; #21 с верным кодом → `400`, `CODE` нетронут `attempts:0`, пароль не изменён). Регрессия без потерь: AC-1/2/3 anti-enumeration, AC-4/5 смена пароля, AC-6 одноразовость, AC-7 TTL, AC-11 инвалидация JWT (`tokenVersion 1→2`, success-path гейта не сломал бамп), AC-12 политика пароля. Остаточных блокирующих `[BUG]` нет. Не гонялся динамический конкурентный PoC (за параллельной security-перепроверкой). qa→done; обе проверки (qa PASS + security PASS-WITH-NOTES) пройдены → deploy готов к старту. (security-строку не трогал.)
