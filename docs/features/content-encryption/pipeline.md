# Pipeline

- **feature:** content-encryption
- **created:** 2026-06-30

## Этапы
Статусы: `pending` → `ready` → `in_progress` → `done` · `blocked` · `failed`

| Этап | Роль | Статус | Артефакт |
|---|---|---|---|
| analyze | business-analyst | done    | 01-requirements.md |
| design  | architect        | done    | 02-spec.md |
| build   | dev-lead         | done    | 03-build-notes.md + frontend/ backend/ |
| qa      | qa-engineer      | done    | 04-test-report.md |
| security| security-engineer| done    | 04b-security-review.md |
| deploy  | devops           | done    | 05-deploy.md |

## Журнал
- 2026-06-30 intake: фича заведена. Защита чувствительного контента дневника шифрованием (сейчас в DynamoDB plaintext). Ключевой открытый вопрос — at-rest (KMS/поле) vs end-to-end.
- 2026-06-30 analyze: написан 01-requirements.md. 14 FR, 12 AC (Given/When/Then). Перечислены шифруемые контентные поля и нешифруемые метаданные; сохранена модель приватности (free_thought личное, note_to_partner общее); описана стратегия миграции (ленивая on-write + идемпотентный разовый прогон). 7 допущений. 1 [BLOCKER] для PO: выбор модели угроз at-rest vs E2E (рекомендация BA — at-rest server-side/KMS envelope для v1). analyze → done, design → ready.
- 2026-06-30 PO-решение: [BLOCKER] снят — выбран **at-rest server-side (KMS envelope)**. E2E отклонён как неподходящий для PWA (мульти-устройство, сброс пароля = потеря данных, push без текста, шаринг). E2E — кандидат на отдельную будущую фичу. Архитектор проектирует at-rest server-side.
- 2026-06-30 design: написан 02-spec.md. Решение — envelope encryption (1 KMS CMK + локальный AES-256-GCM с кэшем data key), формат поля Map `{enc_v,iv,tag,ct,dk}`, маркер `enc_v` для версии/миграции. Шифрование изолировано в новой либе `backend/src/lib/crypto.ts`; затронуты entries/save (encrypt), entries/get (decrypt, free_thought партнёру не дешифруется), qualities create/update/list. Контракты API/клиент не меняются. Инфра: CMK+alias+IAM в template.yaml. Миграция: ленивая on-write + идемпотентный backfill-скрипт (прод-запуск = [NEEDS-CEO-APPROVAL]). Локальный fake-KMS режим для DynamoDB Local. 9 задач T-1..T-9. ADR 0001 создан. [NEEDS-BA] нет. design → done, build → ready.
- 2026-06-30 build: реализованы все T-1..T-9 в ветке `feat/content-encryption` (атомарные коммиты). Новая либа `backend/src/lib/crypto.ts` (envelope KMS/AES-256-GCM + кэш DEK + fake-KMS), секция `crypto` в app.config, encrypt в entries/save и qualities/create+update, decrypt в entries/get (free_thought партнёру не дешифруется) и qualities/list, KMS CMK+alias+least-privilege IAM в template.yaml (`sam validate --lint` ок), идемпотентный backfill `scripts/migrate-encrypt-content.ts`, fake-KMS env в `.env.example`. Backend typecheck зелёный; контракты API/фронт не менялись. Прогнаны handler-level integration-проверки против DynamoDB Local (21/21 PASS, AC-1..AC-12 + qualities) и тест миграции (AC-8: run1 шифрует, run2 no-op). Backfill над прод НЕ запускался — [NEEDS-CEO-APPROVAL] на DEPLOY. 2 мелких [SPEC-DEVIATION] (сигнатуры encrypt/decryptField; Retain-политика CMK) — в 03-build-notes. build → done, qa → ready.
- 2026-06-30 qa: написан 04-test-report.md. Вердикт **PASS** — все 12 AC закрыты (12/12 PASS, 0 FAIL). E2E против полного локального стека (DynamoDB Local + fake-KMS, `npm run local`, curl): доказан ciphertext-at-rest (поля → Map `{enc_v,iv,tag,ct,dk}`, метаданные plaintext) и plaintext-round-trip через API. Приватность: партнёр видит расшифрованный `note_to_partner`, `free_thought` не дешифруется и отсутствует в ответе; `shared===false` → запись не отдаётся. Проверены ленивая on-write миграция (AC-7), идемпотентный backfill (AC-8: run1 шифрует 3 поля, run2 no-op), пустой `mood_text` не становится ciphertext (AC-9), устойчивость к битому полю + лог `decrypt_failed` без контента (AC-12), qualities create/update/list, календарь без дешифрования (FR-14), IAM least-privilege в template.yaml. Backend typecheck / frontend build / `sam validate` зелёные. Дефектов нет; 3 info-наблюдения (DEK reuse в тёплом контейнере — by design; notify-partner 500 локально из-за SSM/VAPID — env-ограничение; пустая строка хранится как plaintext ""). Не протестировано: реальный KMS/Web Push/прод-backfill (требуют прод-ключей, этап DEPLOY). qa → done; deploy остаётся ready.
- 2026-06-30 security: написан 04b-security-review.md. Вердикт **PASS-WITH-NOTES** (нет critical/high). Проверены крипто-корректность (AES-256-GCM, уникальный IV/поле, проверка auth tag, DEK 256-бит, wrapped DEK без plaintext в хранилище/логах), приватность полей (`free_thought` не дешифруется и не отдаётся партнёру ни в одном code path; `note_to_partner`/`shared===false` ок), IDOR (userId только из проверенного JWT), IAM least-privilege на конкретный CMK + Retain/PendingWindow, миграция идемпотентна и без логирования plaintext, деградация при сбое KMS/битом поле без утечки. 4 находки: 1×medium (нет жёсткого guard'а против fake-KMS в проде — `crypto.ts:122`/`app.config.ts:58`), 3×low (хардкод dev-мастер-ключа `crypto.ts:96`; длина ciphertext = длина plaintext; `console.error(err)` в миграции). Блокеров деплоя нет. security → done, deploy → ready. SEC-1/SEC-2 закрыты harden-фиксом (fail-fast guard + убран dev-fallback ключ) — подтверждено в коде на этапе deploy.
- 2026-06-30 deploy: написан 05-deploy.md. Подготовка завершена, боевой деплой НЕ выполнялся (ждёт `[NEEDS-CEO-APPROVAL]`). Объём — backend+infra only (фронт фичей не затронут, `deploy-frontend.sh` не нужен). Readiness-проверки зелёные: `sam validate --lint` (valid), `tsc -p backend` (exit 0), `cd frontend && npm run build` (1771 modules), `sam build` (Build Succeeded), скан секретов в файлах фичи — чисто. Новых SSM-параметров нет: CMK создаётся в стеке, `KMS_KEY_ID` через `!Ref`, `STAGE=prod` через `!Ref Stage` default; `KMS_LOCAL`/`DYNAMO_ENDPOINT` в проде НЕ задаются (иначе fail-fast guard уронит cold start save/get/qualities — by design, SEC-1 закрыт). Подготовлены 2 «зелёные кнопки» под одобрение PO: (1) `bash scripts/deploy-backend.sh` — создаёт CMK `alias/diary-content-prod` (Retain, ротация), включает ленивое шифрование on-write; (2) backfill `scripts/migrate-encrypt-content.ts` (dry-run → real) над `DiaryMain-prod` — изменение прод-данных, идемпотентно. Откат: безопасен только ДО первого зашифрованного сохранения; после — код-откат делает данные нечитаемыми (forward-fix only), CMK с Retain НЕ удалять. Стоимость ≈ $1/мес (CMK) + доли цента запросы. ⚠️ Pre-gate в ранбуке: в рабочем дереве есть посторонние незакоммиченные правки (calendar.ts/notify-partner.ts/webpush.ts/frontend) НЕ из этой фичи — деплоить только с чистого дерева ревьюнутого коммита, иначе протащит непроверенные изменения. deploy → done (подготовлено, ждёт одобрения PO).
